<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
    <h3><%= Helpers.current_user(session).username %></h3>
    <h3><%= Helpers.current_user(session).balance %></h3>
    <h2><a href="/logout">Log Out</a></h2>
    <% end %>
  </body>
</html>